// frontend/src/pages/ChequesPage.jsx
import React from 'react';
import { Typography } from 'antd';
const { Title } = Typography;

const ChequesPage = () => (
    <Title level={2}>صفحه مدیریت چک‌ها (در حال ساخت)</Title>
);

export default ChequesPage;