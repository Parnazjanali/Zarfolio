// frontend/src/pages/TransfersPage.jsx
import React from 'react';
import { Typography } from 'antd';
const { Title } = Typography;

const TransfersPage = () => (
    <Title level={2}>صفحه مدیریت انتقال‌ها (در حال ساخت)</Title>
);

export default TransfersPage;